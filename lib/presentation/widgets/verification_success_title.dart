import 'package:flutter/material.dart';

class VerificationSuccessTitle extends StatelessWidget {
  const VerificationSuccessTitle({super.key});

  @override
  Widget build(BuildContext context) {
    return Text(
      'Ready to Rule!',
      style: TextStyle(fontSize: 30, fontWeight: FontWeight.w800),
    );
  }
}
