import 'package:flutter/material.dart';

class OtpPageTitle extends StatelessWidget {
  const OtpPageTitle({super.key});

  @override
  Widget build(BuildContext context) {
    return Text(
      'OTP Verification',
      style: TextStyle(fontSize: 30, fontWeight: FontWeight.w800),
    );
  }
}
